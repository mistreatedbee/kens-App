# URL of the backend API (leave empty in development — Vite proxy handles it)
# In production, set this to your deployed backend URL, e.g. https://your-app.onrender.com
VITE_API_URL=

# Admin API key — must match ADMIN_API_KEY in server/.env
VITE_ADMIN_API_KEY=your_secret_admin_key_here
